<html lang="en">
<head>

    <link rel="stylesheet" href="./styles.css"/>
</head>
<body>
    <div class="body-wrapper">
        <div class="userDiv">
            <img class="profilePic" src={{avatar}}/>
            <div class="user-info">
                <div>{{name}}</div>
                <div>{{state}}, {country} | {{job}} {{company}}</div>
                <div>{{email}} | {{phone}}</div>
            </div>
                <p class="lookingForWork"> {{^job}}Available for Hire{{/job}}</p>
            </div>
        </div>
    </div>
</body>
</html>